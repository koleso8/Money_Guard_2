export const selectIsError = state => state.transactions.error;

export const selectIsLoading = state => console.log(state);

// state.transactions.loading;
export const selectContacts = state => state.transactions.items;

// export const selectCurrent = state => state.editing.current;
